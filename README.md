# Focus-Dial
Focus Dial — a self-hosted focus timer, no framework, no build step. Instead of a generic progress ring, you set the time by turning a brass mechanical dial (drag it, or tab to it and use arrow keys). Start/pause/reset a session, a short synthesized chime plays on completion, and finished sessions get logged locally in the browser.
